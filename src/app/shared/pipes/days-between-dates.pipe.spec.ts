import { DaysBetweenDatesPipe } from './days-between-dates.pipe';

describe('DaysBetweenDatesPipe', () => {
  it('create an instance', () => {
    const pipe = new DaysBetweenDatesPipe();
    expect(pipe).toBeTruthy();
  });
});
