import { HideTabsDirective } from './hide-tabs.directive';

describe('HideTabsDirective', () => {
  it('should create an instance', () => {
    const directive = new HideTabsDirective();
    expect(directive).toBeTruthy();
  });
});
