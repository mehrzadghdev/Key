import { PercentageInputDirective } from './percentage-input.directive';

describe('PercentageInputDirective', () => {
  it('should create an instance', () => {
    const directive = new PercentageInputDirective();
    expect(directive).toBeTruthy();
  });
});
